plugins {
    id "io.micronaut.build.internal.redis-base"
    id "io.micronaut.application"
}

tasks.named("test") {
    useJUnitPlatform()
}

graalvmNative {
    toolchainDetection = false
}
micronaut {
    runtime("netty")
}
